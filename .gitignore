build/
build-lint/
build-sanitize/
build-arm64/
build-x86_64/
build-win/
build-mac-fast/
dist/
dist-win/
download/
__pycache__/
sdk/
vendor/
.idea/
.cache/
*.xpl
*.o
*.a
*.dylib
.DS_Store
data/imgui.ini
CMakeFiles/
CMakeCache.txt
cmake_install.cmake
Makefile.cmake

# Local inference models — downloaded via setup, never committed (see milestone 02–04)
Resources/models/

# stray/aux dirs (not part of the plugin)
build-pr/
/memory/

# Leftover whisper.cpp / llama.cpp / piper1-gpl checkouts from before those
# submodules were dropped (they are prebuilt in xp_wellys_libs now). Harmless
# to keep locally, but untracked they would be ~240 MB for `git add -A` to
# swallow into the repo. Safe to delete.
spikes/*/third_party/
